# Rendered HTML outputs — users render these themselves
example.html
poster-minimal.html
poster-about.html
poster-about-dense.html

# Any other rendered HTML at root level
*.html

# Quarto render cache and output directories
.quarto/
*_files/
*_cache/

# Quarto freeze directory
_freeze/

# OS files
.DS_Store
Thumbs.db

# VS Code workspace settings (optional — remove if you want to share them)
.vscode/

# Lua language server config — keep or ignore depending on preference
# .luarc.json
/.luarc.json
